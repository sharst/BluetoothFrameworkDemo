<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.uos.nbp.bluetoothconnectiondemo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="10" />

    
	<uses-permission android:name="android.permission.BLUETOOTH"/>
	<uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
	
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:theme="@android:style/Theme.NoTitleBar" 
        android:label="@string/app_name">
        <activity
            android:name="de.bruewer.bluetoothtesting.BluetoothTestActivity"
            android:label="BluetoothTest" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="de.uos.nbp.senhance.bluetooth.DeviceListActivity" />
    </application>

</manifest>
